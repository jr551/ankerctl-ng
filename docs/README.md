# Docs

This repo now treats the root [README](../README.md) as the main entry point.

Use that first for:

- install
- update
- Docker
- OrcaSlicer setup

The rest of this folder is mostly reference material from the original rewrite effort:

- `protocols/` for PPPP and API notes
- `operations/` for firewall and host setup
- `wiki/` and `archive/` for older documentation
